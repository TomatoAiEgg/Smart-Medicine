# 上下文管理

本文档用于新对话继续接手本项目。换新对话框后，先阅读本文档，再按“继续任务”的顺序执行。

## 1. 当前工作目录

```text
D:\workspace\包装项目\智能药房SaaS平台
```

当前目录是 Git 仓库，提交前必须使用 `git status`、`git diff` 和对应验证命令核对变更。

## 2. 必读文档顺序

新对话开始后按以下顺序读取：

1. `docs/00_项目总览/上下文管理.md`
2. `docs/04_开发规范/demand_开发变更与编码规范总则.md`
3. `docs/03_架构设计/架构总蓝图.md`
4. `docs/03_架构设计/总体架构落地设计.md`
5. `docs/02_重构规格/第一阶段编码落地方案.md`
6. `docs/99_项目记录/项目记录.md`
7. `docs/99_项目记录/联调记录.md`
8. 当前要改的模块源码

开发规范来自其他项目，遇到明显不适用于本项目的条款可以忽略，但“先理解上下文、变更留痕、敏感信息不入库、清理前确认”必须遵守。

## 3. 开发环境

本机默认 Java 可能不是 JDK 21。执行 Maven 前先在 PowerShell 中设置：

```text
$env:JAVA_HOME='D:\Javasource\javaTools\jdk-21.0.10'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
```

常用验证命令：

```text
cd backend
mvn test -q
mvn -pl order-service -am test -q
mvn -pl message-service -am test -q
mvn -DskipTests package -q
```

注意：

- 不默认启动本地 Docker Desktop。
- 不默认启动本地 Redis / RocketMQ / PostgreSQL。
- 应用服务默认在本地 IDEA / Maven 启动，连接老笔记本 Ubuntu 中间件。
- 不上传 jar 到服务器运行，除非用户明确要求。
- 当前开发阶段依赖数据库的服务已在 `application.yml` 提供开发默认数据库密码，IDEA 可直接启动；如果配置了 `ZHYF_DB_PASSWORD`，环境变量仍优先。
- `order-service`、`message-service`、`workflow-service` 默认中间件地址已对齐老笔记本：PostgreSQL `100.115.165.51:15432`，RocketMQ NameServer `100.115.165.51:9876`。
- IDEA Run Configuration 中不要保留空值环境变量；空的 `ZHYF_DB_PASSWORD` 会覆盖开发默认值并导致数据库认证失败。`message-service`、`workflow-service` 已对 RocketMQ 空白值做代码级默认回退。

## 4. 中间件与观测环境

当前项目中间件运行在老笔记本 Ubuntu，通过 Tailscale 访问：

| 组件 | 地址 |
| --- | --- |
| Tailscale 主机 | `100.115.165.51` |
| SSH 用户 | `tomatoegg` |
| PostgreSQL | `100.115.165.51:15432` |
| Redis | `100.115.165.51:16379` |
| RocketMQ NameServer | `100.115.165.51:9876` |
| RocketMQ Broker | `100.115.165.51:10911` |
| RocketMQ Dashboard | `http://100.115.165.51:18181` |
| Grafana | `http://100.115.165.51:13000` |
| Loki | `http://100.115.165.51:13100` |
| Prometheus | `http://100.115.165.51:19090` |

SSH 私钥在本机：

```text
C:\Users\13372\Desktop\work\常用key或者id等信息\id_ed25519
```

不要把密码、私钥内容、数据库密码、Grafana 密码写入文档或代码。Grafana 管理员密码在老笔记本 `/home/tomatoegg/zhyf-observability/.env`。

运维文档：

- `docs/06_部署运维/中间件连接信息.md`
- `docs/06_部署运维/本地开发环境.md`
- `docs/06_部署运维/观测中间件.md`

## 5. 当前后端模块

```text
backend
├── zhyf-common
├── gateway
├── auth-institution
├── order-service
├── prescription-service
├── message-service
├── workflow-service
├── ops-service
├── decoction-service
├── logistics-service
├── callback-service
├── portal-service
├── report-service
└── integration-service
```

第一阶段实际承载：

- `gateway`：机构推单入口、签名/IP 校验、访问日志、转发到 `order-service`。
- `auth-institution`：机构应用和 IP 白名单内部查询。
- `order-service`：机构推单、订单/处方/明细落库、状态日志、Outbox、订单查询、订单履约进度查询、内部订单状态推进接口。
- `message-service`：Outbox 发布、RocketMQ 生产者、RocketMQ 消费入口、消费幂等、`ORDER_CREATED` 校验记录。
- `workflow-service`：消费校验通过的 `ORDER_CREATED` 创建待审核任务、审核任务查询、审核通过/拒绝、审核通过后创建调剂任务、调剂完成写调剂记录并创建复核任务、复核完成时调用 `order-service` 推进订单到 `RECHECKED`。
- `prescription-service`：当前只有模块骨架，默认本地端口 `18084`，处方表第一阶段暂由 `order-service` 承载。
- `ops-service`：默认本地端口 `18086`，已提供只读排错查询接口，用于联调查看 Outbox、消费日志、订单校验记录、网关访问日志、物流/回调失败原因聚合、集成重试失败聚合和核心健康概览；`admin-web` 已接入“运维排错”页。
- `decoction-service`：默认本地端口 `18087`，已提供 PDA/MES 模拟器第一版，并已通过真实本地联调验证从复核完成处方绑定设备、开始煎煮、完成煎煮并推进订单状态；当前已补加水完成、温度、异常事件记录、取消/终止、设备释放规则、非法状态拒绝事件记录、设备作业明细、真实设备 adapter 抽象和旧 PDA/MES 协议兼容入口。
- `logistics-service`：默认本地端口 `18088`，已提供物流/自提最小链路：待打包订单查询、打包、发货、签收、顺丰/EMS 轨迹回调兼容入口、物流轨迹查询，并通过 `order-service` 内部接口推进订单到 `PACKED/SHIPPED/IN_TRANSIT/SIGNED`。
- `callback-service`：默认本地端口 `18089`，已提供机构回调记录补偿模型：创建 `callback_record`、真实 HTTP 回调发送、到期回调手动派发、可配置定时派发、失败重试、`DEAD` 终态、人工标记成功/失败和重放；定时派发默认关闭，避免开发环境误触发外部接口。
- `portal-service`：默认本地端口 `18090`，已提供医院查单和地址补录申请最小入口；查单要求订单号/外部单号 + 患者或收件人手机号匹配，地址补录只写 `portal_address_supplement.PENDING`，不直接修改订单地址。
- `report-service`：默认本地端口 `18091`，已提供报表统计最小只读入口和 CSV 导出；当前聚合订单、处方、物流单、回调、待处理地址补录、订单状态分布、回调状态分布和近期订单趋势，不新增业务表、不影响订单主链路。
- `integration-service`：默认本地端口 `18092`，已提供社康消息、社康状态回写、地址补录回推和医院处方查单的协议适配入口；当前通过 `integration_message` 归档外围消息，通过 `integration_retry_task` 承载真实 HTTP 回推、失败重试、`DEAD` 终态和手动派发，不直接改订单主链路。

## 6. 当前已完成链路

当前最小链路已经推进到：

```text
机构推单
 -> gateway 签名/IP/访问日志
 -> order-service 创建订单
 -> order-service 创建处方和处方明细
 -> order_status_log 写 CREATED
 -> event_outbox 写 ORDER_CREATED
 -> message-service 发布 RocketMQ
 -> message-service 消费 ORDER_CREATED
 -> message_consume_log 幂等记录
 -> order_validation_record 写 PASSED/REJECTED
 -> workflow-service 查询校验记录并创建 workflow_task: ORDER_REVIEW/PENDING
 -> workflow-service 查询待审核任务
 -> workflow-service 审核通过/拒绝
 -> order-service 内部接口推进 order_main.status=AUDIT_PASSED/AUDIT_FAILED
 -> order_status_log 写审核状态变化
 -> 审核通过后 workflow-service 创建 workflow_task: PRESCRIPTION_DISPENSE/PENDING
 -> admin-web 查询调剂任务
 -> workflow-service 完成调剂并写 dispense_record
 -> workflow-service 创建 workflow_task: PRESCRIPTION_RECHECK/PENDING
 -> admin-web 查询复核任务
 -> workflow-service 完成复核
 -> order-service 内部接口推进 order_main.status=RECHECKED
 -> order_status_log 写复核状态变化
 -> admin-web 煎煮模拟页查询可操作处方和设备
 -> decoction-service 创建 decoction_task: BOUND
 -> MES/PDA 模拟开始煎煮
 -> order-service 内部接口推进 order_main.status=DECOCTING
 -> MES/PDA 模拟完成煎煮
 -> order-service 内部接口推进 order_main.status=DECOCTED
 -> order_status_log 写煎煮状态变化
 -> MES 模拟记录加水完成/温度/异常事件到 decoction_task_event
 -> BOUND 任务可取消为 CANCELLED 并释放设备，订单保持 RECHECKED 可重新绑定
 -> DECOCTING 任务可终止为 TERMINATED，并推进订单 DECOCTING -> CANCELLED
 -> 非法状态/乱序上报会被拒绝，并写入 decoction_task_event: REJECTED
 -> 成功和拒绝的设备动作写入 decoction_device_work_record 结构化作业明细
 -> admin-web 运维排错页可查询 Outbox、消费日志、订单校验记录和网关访问日志
 -> 旧 PDA/MES 协议入口复用 decoction-service adapter 映射到同一套煎煮业务服务
 -> admin-web 物流回调页查询 DECOCTED 待打包订单
 -> logistics-service 创建 shipment: PACKED，并推进订单 DECOCTED -> PACKED
 -> logistics-service 发货/签收或接收顺丰/EMS 轨迹回调，写 shipment_trace
 -> order-service 内部接口推进订单 PACKED -> SHIPPED/IN_TRANSIT/SIGNED
 -> callback-service 创建 callback_record，支持真实 HTTP 派发、失败重试、DEAD 终态和人工成功/失败/重放
 -> admin-web 物流回调页可查询物流单、轨迹和回调补偿记录
 -> portal-service 支持医院查单和地址补录申请
 -> report-service 提供只读报表统计，admin-web 报表统计页展示核心指标和状态分布
 -> integration-service 记录社康消息、地址回推记录，并按处方号 + 手机号兼容查询医院订单
 -> integration-service 对地址补录回推和社康状态回写创建 integration_retry_task，支持真实 HTTP 派发、失败重试和 DEAD 终态
 -> admin-web 集成适配页可登记外围消息、查询处方订单、查看集成消息列表和手动派发到期重试任务
 -> ops-service/admin-web 运维排错页可聚合查询 FAILED/DEAD 集成重试任务，定位来源系统、业务键、失败响应和下次重试时间
```

已验证：

- `mvn test -q` 通过。
- `mvn -pl order-service -am test -q` 通过。
- `mvn -pl message-service -am test -q` 通过。
- `mvn -pl workflow-service -am test -q` 通过。
- `mvn -DskipTests package` 通过。
- `pnpm build` 通过。
- 2026-06-25 已完成真实端到端 HTTP 联调：本地启动 `gateway`、`auth-institution`、`order-service`、`message-service`，连接老笔记本 PostgreSQL/RocketMQ，验证 `gateway` 推单、Outbox 发布、RocketMQ 消费、校验记录、待审核任务、审核通过到 `AUDIT_PASSED` 全链路。
- 真实联调中修复 `gateway` 构造器注入问题和 `order-service` PostgreSQL `timestamptz` 映射问题，`mvn test -q` 已重新通过。
- 2026-06-25 已完成 `workflow-service` 代码层迁移和单元/打包验证：`message-service` 不再直接创建 `workflow_task`，`workflow-service` 接管任务创建与审核接口，`order-service` 提供内部状态推进接口。
- 2026-06-25 已根据 `docs/error` 修复 IDEA 启动侧代码问题：RocketMQ 配置空白值回退、`prescription-service:18084`、`ops-service:18086`。
- 2026-06-26 已为依赖数据库的服务补充开发默认数据库密码，避免 IDEA 自动生成 Run Configuration 时缺少 `ZHYF_DB_PASSWORD` 直接启动失败。
- 2026-06-26 已新增 `frontend/admin-web` Vue3 管理端第一版，默认端口 `5174`，提供订单查询和 workflow 审核任务处理。
- 2026-06-26 已将架构落地文档口径从“第一阶段架构落地设计”调整为“总体架构落地设计”，阶段一、二、三只作为总体架构内的实施路线。
- 2026-06-26 已完成 `order-service` 订单状态机封装，`OrderStatusUpdateService` 和旧过渡 `OrderReviewTaskService` 均通过 `OrderStatusMachine` 校验状态流转。
- 2026-06-26 已完成调剂/复核最小模型第一步：审核通过后自动创建 `PRESCRIPTION_RECHECK/PENDING` 任务，`admin-web` 可查询复核任务并完成复核，订单推进到 `RECHECKED`。
- 2026-07-18 已补齐核心调剂任务闭环：审核通过后创建 `PRESCRIPTION_DISPENSE/PENDING`，`admin-web` 可查询并完成调剂，`workflow-service` 写入 `dispense_record` 后再创建 `PRESCRIPTION_RECHECK/PENDING`，复核完成仍推进到 `RECHECKED`；社康配置化拉单暂缓。
- 2026-07-18 已补齐核心后台可用性收口：`order-service` 新增订单履约进度查询，`report-service` 新增报表 CSV 导出，`ops-service` 新增核心健康概览，`admin-web` 分别接入订单详情、报表统计和运维排错页。
- 2026-06-26 已完成 `ops-service` 最小只读查询接口，覆盖 `event_outbox`、`message_consume_log`、`order_validation_record`、`api_access_log`。
- 2026-06-26 已完成 `decoction-service` PDA/MES 模拟器第一版：`decoction_task` 记录绑定/开始/完成，PDA 查询可操作处方和设备，MES 查询活动任务并上报开始/完成，订单可推进到 `DECOCTING/DECOCTED`。
- 2026-06-26 已在 `admin-web` 增加“煎煮模拟”入口，默认通过 Vite proxy 转发到 `decoction-service:18087`。
- 2026-06-26 已完成 PDA/MES 煎煮链路真实本地联调：六个临时服务 `19080/19081/19082/19083/19085/19087` 跑通网关推单、事件发布消费、审核、复核、PDA 绑定、MES 开始/完成，测试订单 `ZHYF1782473402454` 最终到 `DECOCTED`。
- 2026-06-26 已完成 PDA/MES 事件日志增强：新增 `decoction_task_event`，MES 支持加水完成、温度、异常记录和事件查询，`admin-web` 已提供对应操作入口。
- 2026-06-26 已完成 PDA/MES 取消/终止状态流转：`BOUND -> CANCELLED` 释放设备但订单保持 `RECHECKED`；`DECOCTING -> TERMINATED` 释放设备并推进订单 `DECOCTING -> CANCELLED`；成功取消/终止均写 `decoction_task_event`。
- 2026-06-27 已完成 PDA/MES 非法状态/乱序上报拒绝记录持久化：开始、完成、取消、终止、加水完成、温度上报在状态不匹配时仍拒绝业务操作，但会写 `decoction_task_event.event_type=REJECTED`，payload 记录动作、来源、原始 `operationId`、实际状态、期望状态和原因。
- 2026-06-27 已完成 PDA/MES 设备作业明细模型：新增 `decoction_device_work_record`，绑定、开始、完成、取消、终止、加水完成、温度、异常和拒绝动作均写结构化明细；`admin-web` 可按任务查看作业明细。
- 2026-06-27 已把 `ops-service` 接入 `admin-web` 运维排错页：新增 `/ops-api` Vite proxy，可按 Outbox、消费日志、订单校验、访问日志四类数据查询和筛选。
- 2026-07-07 已完成 PDA/MES 真实设备 adapter 入口第一版：新增 `DeviceAdapter`、`MesAdapter` 抽象和模拟实现，新增旧协议兼容接口 `/pdaUserLogin`、`/pdaGetDecoctEquip`、`/pdaGetCanOpRecipeList`、`/pdaBindPrescription`、`/pdaOpDecoctStatus`、`/getWaterByPailNo`、`/waterEndNotice`、`/getRecipeInfoById`、`/recipeBoilInfoCallback`、`/reviewFinishPushRecipe`、`/decoctingStatusNotice`。
- 2026-07-07 已完成物流/回调补偿最小链路：新增 `logistics-service:18088`、`callback-service:18089`、Flyway `V8__add_logistics_callback.sql`，`admin-web` 新增“物流回调”入口，支持待打包订单、物流单、轨迹和回调记录查询，以及打包/发货/签收/轨迹录入/回调人工补偿。
- 2026-07-07 已完成 `callback-service` 真实 HTTP 回调发送和重试调度：新增 `CallbackSender`、`HttpCallbackSender`、`CallbackDispatchJob`、到期回调查询、`DEAD` 终态、手动派发接口和 `admin-web`“派发到期回调”按钮；定时派发通过 `ZHYF_CALLBACK_DISPATCH_ENABLED=true` 开启。
- 2026-07-08 已完成 `ops-service` 物流/回调失败原因聚合查询：新增 `GET /api/admin/ops/logistics-callback-issues`，默认查询 `FAILED/DEAD` 回调，并关联物流单、订单号和最新轨迹；`admin-web` 运维排错页新增“回调失败”入口。
- 2026-07-08 已完成物流/回调本地端到端联调：基于订单 `ZHYF1782473402454` 跑通打包、发货、运输中、签收、真实 HTTP 失败派发、人工重放、`DEAD` 死信终态和 `ops-service` `FAILED/DEAD` 聚合查询；联调中修复 `logistics-service`、`callback-service` 写入 PostgreSQL `timestamptz` 时直接传 `Instant` 的 JDBC 类型问题，并修正 `ops-service` 聚合查询按 `shipmentId` / `shipmentId:状态` 关联回调记录。
- 2026-07-09 已完成 admin-web + ops-service + decoction-service + 旧协议入口本地联调：测试订单 `ZHYF1783574426206`、处方 `RX17835744275931` 通过旧 PDA/MES 入口完成绑定、加水、开始、温度和完成煎煮，最终 `order_main.status=DECOCTED`、`decoction_task.task_status=DECOCTED`；`admin-web` `/decoction-api` 和 `/ops-api` 代理可查询事件、作业明细和 Outbox；联调中修复 `decoction-service` 写入 PostgreSQL `timestamptz` 时直接传 `Instant` 的 JDBC 类型问题。
- 2026-07-09 已完成 `portal-service` 医院查单/地址补录最小入口：新增 `portal-service:18090`、Flyway `V9__add_portal_address_supplement.sql` 和 `admin-web`“门户查单”入口；本地真实验证使用订单 `ZHYF1783574426206` + 手机号查询到 `DECOCTED` 订单和 1 条处方，并写入 `portal_address_supplement.PENDING` 申请 `6b2e2958-4300-4d3f-8643-f76badaf15ab`。
- 2026-07-09 已完成 `report-service` 报表统计最小链路：新增 `report-service:18091` 和 `admin-web`“报表统计”入口；本地真实验证 `GET /api/admin/reports/overview?trendDays=14` 返回业务码 `0`，当前库统计到订单 6、处方 6、物流单 1、回调 4、待处理地址补录 1。
- 2026-07-09 已完成 `integration-service` 外围协议适配最小链路：新增 `integration-service:18092`、Flyway `V10__add_integration_message.sql` 和 `admin-web`“集成适配”入口；本地真实验证社康消息登记、重复消息幂等、地址回推记录、处方号 `RX17835744275931` + 手机号查询订单 `ZHYF1783574426206`、后台消息列表均返回业务码 `0`。
- 2026-07-09 已完成 `integration-service` 集成重试回推链路：新增 Flyway `V11__add_integration_retry_task.sql`、`integration_retry_task`、真实 HTTP 发送器、到期任务手动派发和可配置定时派发；本地真实验证地址回推和社康状态回写均创建 `PENDING` 重试任务，派发到本机 HTTP 接收端后写入 `SUCCESS`。
- 2026-07-17 已完成 `ops-service` 集成重试失败聚合查询：新增 `GET /api/admin/ops/integration-retry-issues`，默认查询 `FAILED/DEAD` 集成任务，并关联 `integration_message` 展示来源系统、消息类型、业务键、失败响应、重试次数和下次重试时间；`admin-web` 运维排错页新增“集成失败”入口。
- 2026-06-25 已完成 `workflow-service` 五服务真实端到端 HTTP 联调：本地临时启动 `gateway`、`auth-institution`、`order-service`、`message-service`、`workflow-service`，验证网关推单、Outbox、RocketMQ、校验记录、workflow 任务创建、workflow 审核通过、订单推进到 `AUDIT_PASSED`。

## 7. 当前关键表

Flyway migration 在 `backend/order-service/src/main/resources/db/migration`：

- `V1__init_core_schema.sql`
- `V2__add_order_validation_record.sql`
- `V3__add_workflow_task.sql`
- `V4__add_decoction_task.sql`
- `V5__add_decoction_task_event.sql`
- `V6__add_decoction_cancel_terminate.sql`
- `V7__add_decoction_device_work_record.sql`
- `V8__add_logistics_callback.sql`
- `V9__add_portal_address_supplement.sql`
- `V10__add_integration_message.sql`
- `V11__add_integration_retry_task.sql`
- `V12__add_dispense_record.sql`

核心表：

- `tenant`
- `institution`
- `institution_app`
- `institution_ip_whitelist`
- `order_main`
- `order_status_log`
- `prescription`
- `prescription_detail`
- `event_outbox`
- `message_consume_log`
- `api_access_log`
- `order_validation_record`
- `workflow_task`
- `dispense_record`
- `decoction_task`
- `decoction_task_event`
- `decoction_device_work_record`
- `shipment`
- `shipment_trace`
- `callback_record`
- `portal_address_supplement`
- `integration_message`
- `integration_retry_task`

注意：`workflow_task` 和 `dispense_record` 当前由 `order-service` Flyway 创建，逻辑归属是 `workflow-service`；`decoction_task`、`decoction_task_event`、`decoction_device_work_record` 当前由 `order-service` Flyway 创建，逻辑归属是 `decoction-service`；`shipment`、`shipment_trace` 逻辑归属是 `logistics-service`，`callback_record` 逻辑归属是 `callback-service`，`portal_address_supplement` 逻辑归属是 `portal-service`，`integration_message` 和 `integration_retry_task` 逻辑归属是 `integration-service`。

## 8. 当前接口

### gateway

- `POST /api/institution/createOrder`

### auth-institution

- `GET /internal/institution-apps/{appKey}`

### order-service

- `POST /api/institution/createOrder`
- `GET /api/admin/orders/{orderNo}`
- `GET /api/admin/orders/{orderNo}/progress`
- `PATCH /internal/orders/{orderId}/status`
- `GET /api/admin/review-tasks`（过渡废弃，后续由 `workflow-service` 接管）
- `PATCH /api/admin/review-tasks/{taskId}/approve`（过渡废弃）
- `PATCH /api/admin/review-tasks/{taskId}/reject`（过渡废弃）

### workflow-service

- `GET /api/admin/workflow/review-tasks`
- `PATCH /api/admin/workflow/review-tasks/{taskId}/approve`
- `PATCH /api/admin/workflow/review-tasks/{taskId}/reject`
- `GET /api/admin/workflow/dispense-tasks`
- `PATCH /api/admin/workflow/dispense-tasks/{taskId}/complete`
- `GET /api/admin/workflow/recheck-tasks`
- `PATCH /api/admin/workflow/recheck-tasks/{taskId}/complete`

### ops-service

- `GET /api/admin/ops/outbox`
- `GET /api/admin/ops/message-consume-logs`
- `GET /api/admin/ops/order-validation-records`
- `GET /api/admin/ops/api-access-logs`
- `GET /api/admin/ops/logistics-callback-issues`
- `GET /api/admin/ops/integration-retry-issues`
- `GET /api/admin/ops/health-overview`

`admin-web` 本地代理路径为 `/ops-api`，默认转发到 `http://127.0.0.1:18086`。

### decoction-service

- `POST /simulator/pda/login`
- `GET /simulator/pda/prescriptions/can-operate`
- `GET /simulator/pda/decoction/devices`
- `POST /simulator/pda/bind-prescription`
- `POST /simulator/pda/decoction/start`
- `POST /simulator/pda/decoction/finish`
- `POST /simulator/pda/decoction/cancel`
- `POST /simulator/pda/decoction/terminate`
- `GET|POST /pdaUserLogin`
- `GET|POST /pdaGetDecoctEquip`
- `GET|POST /pdaGetCanOpRecipeList`
- `GET|POST /pdaBindPrescription`
- `GET|POST /pdaOpDecoctStatus`
- `GET /simulator/mes/tasks/pending`
- `GET /simulator/mes/tasks/active`
- `POST /simulator/mes/tasks/{taskNo}/start`
- `POST /simulator/mes/tasks/{taskNo}/finish`
- `POST /simulator/mes/tasks/{taskNo}/cancel`
- `POST /simulator/mes/tasks/{taskNo}/terminate`
- `POST /simulator/mes/tasks/{taskNo}/water-finish`
- `POST /simulator/mes/tasks/{taskNo}/temperature`
- `POST /simulator/mes/tasks/{taskNo}/error`
- `GET /simulator/mes/tasks/{taskNo}/events`
- `GET /simulator/mes/tasks/{taskNo}/work-records`
- `GET|POST /getWaterByPailNo`
- `GET|POST /waterEndNotice`
- `GET|POST /getRecipeInfoById`
- `GET|POST /recipeBoilInfoCallback`
- `GET|POST /reviewFinishPushRecipe`
- `GET|POST /decoctingStatusNotice`

### logistics-service

- `GET /api/admin/logistics/orders/ready`
- `GET /api/admin/logistics/shipments`
- `POST /api/admin/logistics/shipments/pack`
- `PATCH /api/admin/logistics/shipments/{shipmentId}/ship`
- `PATCH /api/admin/logistics/shipments/{shipmentId}/sign`
- `GET /api/admin/logistics/shipments/{shipmentId}/traces`
- `POST /api/admin/logistics/shipments/trace`
- `GET|POST /logistics/notify`
- `GET|POST /logistics/emsNotify`

`admin-web` 本地代理路径为 `/logistics-api`，默认转发到 `http://127.0.0.1:18088`。

### callback-service

- `POST /internal/callback-records`
- `GET /api/admin/callback-records`
- `PATCH /api/admin/callback-records/{id}/mark-success`
- `PATCH /api/admin/callback-records/{id}/mark-failed`
- `PATCH /api/admin/callback-records/{id}/replay`
- `POST /api/admin/callback-records/dispatch-due`

`admin-web` 本地代理路径为 `/callback-api`，默认转发到 `http://127.0.0.1:18089`。

### portal-service

- `GET /api/portal/orders/query`
- `POST /api/portal/orders/{orderNo}/address-supplements`

`admin-web` 本地代理路径为 `/portal-api`，默认转发到 `http://127.0.0.1:18090`。

### report-service

- `GET /api/admin/reports/overview`
- `GET /api/admin/reports/overview.csv`

`admin-web` 本地代理路径为 `/report-api`，默认转发到 `http://127.0.0.1:18091`。

### integration-service

- `POST /api/integration/community/messages`
- `POST /api/integration/address/push-records`
- `POST /api/integration/community/status-pushes`
- `GET /api/integration/hospital/prescriptions/{prescriptionNo}/order`
- `GET /api/admin/integration/messages`
- `GET /api/admin/integration/retry-tasks`
- `POST /api/admin/integration/retry-tasks/dispatch-due`

`admin-web` 本地代理路径为 `/integration-api`，默认转发到 `http://127.0.0.1:18092`。

`order-service` 仍保留机构签名校验，这是过渡期双校验。后续稳定后应把机构鉴权完全前移到 `gateway`。

## 9. 重要代码位置

### order-service

- `backend/order-service/src/main/java/com/zhyf/order/api/InstitutionOrderController.java`
- `backend/order-service/src/main/java/com/zhyf/order/api/InternalOrderController.java`
- `backend/order-service/src/main/java/com/zhyf/order/application/OrderService.java`
- `backend/order-service/src/main/java/com/zhyf/order/application/OrderReviewTaskService.java`
- `backend/order-service/src/main/java/com/zhyf/order/application/OrderStatusUpdateService.java`
- `backend/order-service/src/main/java/com/zhyf/order/domain/OrderStatusMachine.java`
- `backend/order-service/src/main/java/com/zhyf/order/domain/OrderStatusTransition.java`
- `backend/order-service/src/main/java/com/zhyf/order/infrastructure/OrderRepository.java`
- `backend/order-service/src/main/java/com/zhyf/order/domain/OrderSnapshot.java`
- `backend/order-service/src/main/java/com/zhyf/order/domain/WorkflowTaskSnapshot.java`

### message-service

- `backend/message-service/src/main/java/com/zhyf/message/application/OutboxScanJob.java`
- `backend/message-service/src/main/java/com/zhyf/message/application/OrderCreatedEventHandler.java`
- `backend/message-service/src/main/java/com/zhyf/message/consumer/RocketMqEventConsumer.java`
- `backend/message-service/src/main/java/com/zhyf/message/infrastructure/MessageConsumeRepository.java`

### workflow-service

- `backend/workflow-service/src/main/java/com/zhyf/workflow/WorkflowServiceApplication.java`
- `backend/workflow-service/src/main/java/com/zhyf/workflow/api/WorkflowReviewTaskController.java`
- `backend/workflow-service/src/main/java/com/zhyf/workflow/application/OrderCreatedWorkflowService.java`
- `backend/workflow-service/src/main/java/com/zhyf/workflow/application/OrderReviewTaskService.java`
- `backend/workflow-service/src/main/java/com/zhyf/workflow/application/PrescriptionDispenseTaskService.java`
- `backend/workflow-service/src/main/java/com/zhyf/workflow/application/PrescriptionRecheckTaskService.java`
- `backend/workflow-service/src/main/java/com/zhyf/workflow/consumer/RocketMqWorkflowConsumer.java`
- `backend/workflow-service/src/main/java/com/zhyf/workflow/infrastructure/WorkflowTaskRepository.java`
- `backend/workflow-service/src/main/java/com/zhyf/workflow/infrastructure/OrderStatusClient.java`

### gateway

- `backend/gateway/src/main/java/com/zhyf/gateway/route/InstitutionOrderProxyController.java`
- `backend/gateway/src/main/java/com/zhyf/gateway/signature/SignatureVerifier.java`
- `backend/gateway/src/main/java/com/zhyf/gateway/whitelist/IpWhitelistChecker.java`
- `backend/gateway/src/main/java/com/zhyf/gateway/accesslog/AccessLogService.java`

### auth-institution

- `backend/auth-institution/src/main/java/com/zhyf/authinstitution/api/InternalInstitutionAppController.java`
- `backend/auth-institution/src/main/java/com/zhyf/authinstitution/app/InstitutionAppQueryService.java`
- `backend/auth-institution/src/main/java/com/zhyf/authinstitution/infrastructure/InstitutionAppRepository.java`

### ops-service

- `backend/ops-service/src/main/java/com/zhyf/ops/api/OpsQueryController.java`
- `backend/ops-service/src/main/java/com/zhyf/ops/application/OpsQueryService.java`
- `backend/ops-service/src/main/java/com/zhyf/ops/application/OpsRecords.java`
- `backend/ops-service/src/main/java/com/zhyf/ops/infrastructure/OpsQueryRepository.java`

### admin-web

- `frontend/admin-web/src/App.vue`
- `frontend/admin-web/src/api/workflow.ts`
- `frontend/admin-web/src/api/ops.ts`
- `frontend/admin-web/src/api/logistics.ts`
- `frontend/admin-web/src/api/callback.ts`
- `frontend/admin-web/src/api/portal.ts`
- `frontend/admin-web/src/api/report.ts`
- `frontend/admin-web/src/api/integration.ts`
- `frontend/admin-web/src/api/types.ts`
- `frontend/admin-web/vite.config.ts`

### decoction-service

- `backend/decoction-service/src/main/java/com/zhyf/decoction/api/PdaSimulatorController.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/api/MesSimulatorController.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/api/legacy/LegacyPdaProtocolController.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/api/legacy/LegacyMesProtocolController.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/adapter/DeviceAdapter.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/adapter/MesAdapter.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/adapter/SimulatorDeviceAdapter.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/adapter/SimulatorMesAdapter.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/application/DecoctionEventCommand.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/application/DecoctionRejectedEventRecorder.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/application/DecoctionSimulatorService.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/domain/DecoctionTaskStatus.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/infrastructure/DecoctionTaskRepository.java`
- `backend/decoction-service/src/main/java/com/zhyf/decoction/infrastructure/OrderStatusClient.java`

### logistics-service

- `backend/logistics-service/src/main/java/com/zhyf/logistics/api/LogisticsAdminController.java`
- `backend/logistics-service/src/main/java/com/zhyf/logistics/api/LegacyLogisticsController.java`
- `backend/logistics-service/src/main/java/com/zhyf/logistics/application/LogisticsService.java`
- `backend/logistics-service/src/main/java/com/zhyf/logistics/application/LogisticsCommands.java`
- `backend/logistics-service/src/main/java/com/zhyf/logistics/application/LogisticsRecords.java`
- `backend/logistics-service/src/main/java/com/zhyf/logistics/infrastructure/LogisticsRepository.java`
- `backend/logistics-service/src/main/java/com/zhyf/logistics/infrastructure/OrderStatusClient.java`
- `backend/logistics-service/src/main/java/com/zhyf/logistics/infrastructure/CallbackClient.java`

### callback-service

- `backend/callback-service/src/main/java/com/zhyf/callback/api/CallbackController.java`
- `backend/callback-service/src/main/java/com/zhyf/callback/application/CallbackService.java`
- `backend/callback-service/src/main/java/com/zhyf/callback/application/CallbackDispatchJob.java`
- `backend/callback-service/src/main/java/com/zhyf/callback/application/CallbackProperties.java`
- `backend/callback-service/src/main/java/com/zhyf/callback/application/CallbackSender.java`
- `backend/callback-service/src/main/java/com/zhyf/callback/application/CallbackSendResult.java`
- `backend/callback-service/src/main/java/com/zhyf/callback/application/CallbackCreateCommand.java`
- `backend/callback-service/src/main/java/com/zhyf/callback/application/CallbackRecords.java`
- `backend/callback-service/src/main/java/com/zhyf/callback/infrastructure/HttpCallbackSender.java`
- `backend/callback-service/src/main/java/com/zhyf/callback/infrastructure/CallbackRepository.java`

### portal-service

- `backend/portal-service/src/main/java/com/zhyf/portal/api/PortalController.java`
- `backend/portal-service/src/main/java/com/zhyf/portal/application/PortalQueryService.java`
- `backend/portal-service/src/main/java/com/zhyf/portal/application/PortalCommands.java`
- `backend/portal-service/src/main/java/com/zhyf/portal/application/PortalRecords.java`
- `backend/portal-service/src/main/java/com/zhyf/portal/infrastructure/PortalRepository.java`

### report-service

- `backend/report-service/src/main/java/com/zhyf/report/api/ReportController.java`
- `backend/report-service/src/main/java/com/zhyf/report/application/ReportQueryService.java`
- `backend/report-service/src/main/java/com/zhyf/report/application/ReportRecords.java`
- `backend/report-service/src/main/java/com/zhyf/report/infrastructure/ReportQueryRepository.java`

### integration-service

- `backend/integration-service/src/main/java/com/zhyf/integration/api/IntegrationController.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/api/IntegrationAdminController.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/application/IntegrationService.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/application/IntegrationDispatchJob.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/application/IntegrationProperties.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/application/IntegrationTaskSender.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/application/IntegrationSendResult.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/application/IntegrationCommands.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/application/IntegrationRecords.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/infrastructure/HttpIntegrationTaskSender.java`
- `backend/integration-service/src/main/java/com/zhyf/integration/infrastructure/IntegrationRepository.java`

## 10. 下一步任务建议

优先级从高到低：

1. 增加订单状态机封装，避免后续直接随意更新 `order_main.status`。
   - 已完成：`CREATED -> AUDIT_PASSED/AUDIT_FAILED/CANCELLED`、`AUDIT_PASSED -> RECHECKED/CANCELLED`、`RECHECKED -> DECOCTING/CANCELLED`、`DECOCTING -> DECOCTED/CANCELLED`，后续煎煮物流主链路已集中定义。
2. 调剂和复核最小模型：
   - 已完成：审核通过自动创建调剂任务，调剂完成写 `dispense_record` 并创建复核任务，复核完成推进 `AUDIT_PASSED -> RECHECKED` 并写订单状态日志。
   - 后续可再拆处方级状态和更细的打单/称重记录。
3. `ops-service` 最小查询接口：
   - 已完成：`event_outbox`、`message_consume_log`、`order_validation_record`、`api_access_log` 只读查询。
   - 已完成：`admin-web` 运维排错页接入，可按四类日志查询和筛选。
   - 已完成：物流/回调失败原因聚合查询，可按回调状态、回调类型、物流单号和订单号查看 `callback_record`、`shipment` 与最新 `shipment_trace`。
   - 已完成：集成重试失败聚合查询，可按任务状态、任务类型、业务键和来源系统查看 `integration_retry_task` 与 `integration_message`。
   - 已完成：核心健康概览，聚合 Outbox 待发/失败、消费失败、订单校验拒绝、回调失败/死信、集成重试失败/死信和最近访问量。
4. PDA/MES 模拟器：
   - 已完成第一版、真实本地联调、基础事件日志、取消/终止规则、非法状态拒绝记录、设备作业明细、旧 PDA/MES 协议兼容入口，以及 admin-web + ops-service + decoction-service + 旧协议入口本地联调：可查询复核完成处方、查询模拟设备、绑定处方和设备、MES/PDA 上报开始、完成、取消、终止，并推进订单 `RECHECKED -> DECOCTING -> DECOCTED` 或 `DECOCTING -> CANCELLED`；MES 已支持加水完成、温度、异常记录、拒绝记录、原始事件查询和结构化作业明细查询。
5. 物流/回调补偿最小链路：
   - 已完成：`logistics-service`、`callback-service`、`shipment`、`shipment_trace`、`callback_record`、顺丰/EMS 轨迹兼容入口和 `admin-web` 物流回调页。
   - 已完成：`callback-service` 真实 HTTP 回调发送、手动派发到期回调、可配置定时派发、失败重试和 `DEAD` 终态。
   - 已完成：`ops-service` 对物流/回调失败原因的聚合查询。
   - 已完成真实本地联调：基于已煎煮完成订单跑通打包、发货、运输中、签收、真实失败派发、人工重放、`DEAD` 死信和 `ops-service` `FAILED/DEAD` 聚合查询。
6. 阶段四门户能力：
   - 已完成：`portal-service` 医院查单和地址补录申请最小入口、`portal_address_supplement` 申请表、`admin-web`“门户查单”联调入口。
   - 已完成真实本地验证：临时启动 `order-service:19382` 执行 V9 migration，启动 `portal-service:19390` 查询历史订单并提交地址补录申请。
   - 已完成：`report-service` 报表统计最小只读链路、`admin-web`“报表统计”入口和 `/report-api` 代理。
   - 已完成：`report-service` 报表 CSV 导出，`admin-web` 报表统计页可按当前筛选条件下载。
   - 已完成真实本地验证：临时启动 `report-service:19391`，查询 `GET /api/admin/reports/overview?trendDays=14` 返回订单、处方、物流、回调、地址补录和状态分布统计。
   - 已完成：`integration-service` 社康消息、地址回推记录、医院处方查单最小适配链路、`integration_message` 记录表、`admin-web`“集成适配”入口和 `/integration-api` 代理。
   - 已完成：`integration_retry_task`、地址补录真实 HTTP 回推、社康状态回写重试、手动派发到期任务和可配置定时派发。
   - 已完成真实本地验证：临时启动 `order-service:19482` 执行 V10 migration，启动 `integration-service:19492`，验证社康消息登记/幂等、地址回推记录、处方号 + 手机号查单和后台消息列表。
   - 已完成真实本地验证：临时启动 `order-service:19582` 执行 V11 migration，启动 `integration-service:19592`，使用本机 HTTP 接收端验证地址回推和社康状态回写从 `PENDING` 重试任务派发到 `SUCCESS`。
   - 已完成：`ops-service` 和 `admin-web` 运维排错页聚合集成 `FAILED/DEAD` 重试任务，作为当前失败任务告警入口。

如果新对话只问“继续”，默认先做核心链路真实联调和验收：启动前后端与本地 PostgreSQL/RocketMQ，跑通推单、审核、调剂、复核、订单履约进度、报表导出和运维健康概览；社康配置化拉单/状态回写 adapter 暂缓，等核心链路闭环稳定后再做。

## 11. 禁止事项

- 不要删除老笔记本上其他项目容器。
- 不要误连其他项目的 PostgreSQL、Redis、RocketMQ。
- 不要清库、清日志、删 volume，除非用户明确要求。
- 不要启动 Docker Desktop，除非用户明确要求。
- 不要上传 jar 到服务器运行，除非用户明确要求。
- 不要把密码写进文档、代码或最终回复。
- 不要改旧系统源码，旧项目只读分析。

## 12. 新对话开场提示

新对话可以直接输入：

```text
读取 docs/00_项目总览/上下文管理.md，按里面的下一步继续。
```

如果要继续编码，默认先跑：

```text
$env:JAVA_HOME='D:\Javasource\javaTools\jdk-21.0.10'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
cd backend
mvn test -q
```

测试通过后，再做具体任务。
