# 前端架构重构方案

## 1. 重构目标

前端目标不是重新设计一个和老系统无关的新后台，而是把 `zhyfsupport` 的内部运营后台迁移为 Vue 3 管理端。

核心目标：

- 还原老后台的业务菜单和操作路径。
- 用现代前端结构替代 JSP + iframe + EasyUI。
- 页面围绕真实业务动作设计，不做展示型空壳。
- 前端只调用后端 API，不直接承载业务规则。
- 支持后续权限菜单、状态标签、导入导出、打印、监控跳转。

## 2. 当前问题

当前 `frontend/admin-web` 主要问题：

- 菜单过于简化，没有覆盖老后台模块。
- 页面由 `activeView` 切换，没有真实路由。
- 当前页面更像联调工作台，不像运营后台。
- 订单、审方、调剂、复核、物流、煎煮等页面不符合老系统业务粒度。
- 没有统一的表格、查询表单、分页、详情抽屉、导入导出、打印入口。

## 3. 技术架构

建议技术栈：

- Vue 3
- TypeScript
- Vite
- vue-router
- 原生 CSS 或轻量组件封装
- 后续可按需要引入表格和表单组件库，但第一阶段不强依赖重型 UI 框架

推荐目录：

```text
frontend/admin-web/src
|-- app
|   |-- router.ts
|   |-- menu.ts
|   |-- AppLayout.vue
|-- api
|   |-- client.ts
|   |-- orders.ts
|   |-- workflow.ts
|   |-- decoction.ts
|   |-- logistics.ts
|   |-- reports.ts
|   |-- system.ts
|-- components
|   |-- AdminTable.vue
|   |-- SearchForm.vue
|   |-- StatusTag.vue
|   |-- PageHeader.vue
|   |-- FormDialog.vue
|   |-- ImportDialog.vue
|   |-- PrintAction.vue
|-- features
|   |-- dashboard
|   |-- orders
|   |-- workflow
|   |-- decoction
|   |-- logistics
|   |-- drug
|   |-- institution
|   |-- reports
|   |-- system
|   |-- sms
|   |-- ops
|   |-- auth
|-- domain
|   |-- status.ts
|   |-- dictionaries.ts
|   |-- formatters.ts
```

## 4. 路由和菜单设计

前端必须改为真实路由：

```text
/dashboard
/orders/list
/orders/detail/:orderNo
/orders/review
/orders/adjust
/orders/recheck
/orders/address
/orders/prescription-reprint
/orders/process
/orders/receipt
/decoction/waterpail
/decoction/equipment
/decoction/bind
/decoction/print-records
/logistics/delivery
/logistics/print
/logistics/company
/logistics/special
/logistics/merge
/drug/herbs
/drug/herbs-index
/drug/herbs-area
/institution/list
/institution/api
/institution/whitelist
/reports/prescription
/reports/performance
/reports/reconciliation
/system/dict
/system/config
/system/decoct-center
/system/operator
/sms/list
/ops/mq
/ops/exceptions
/ops/order-process
/auth/users
/auth/roles
/auth/menus
```

菜单来源分两阶段：

- 第一阶段：前端静态菜单，保证页面结构快速落地。
- 第二阶段：后端返回用户菜单和权限点，前端按权限过滤路由和按钮。

## 5. 页面设计原则

- 运营后台以表格和筛选为核心。
- 查询条件必须贴近老系统字段，不用过度抽象。
- 详情页必须显示订单、处方、药品明细、状态日志、操作记录。
- 状态显示用中文业务状态，不直接展示内部枚举或 UUID。
- 行内操作必须靠近数据行。
- 高风险操作使用确认弹窗并展示影响范围。
- 导入导出和打印入口单独封装，避免每个页面重复实现。

## 6. 第一阶段范围

第一阶段只做订单主链路：

- 左侧菜单和真实路由。
- 订单列表。
- 订单详情。
- 审方任务。
- 调剂任务。
- 复核任务。
- 修改地址。
- 处方重打入口。
- 生产进度。
- 通用状态标签、查询表单、分页和详情布局。

第一阶段不做：

- 全量报表。
- 全量权限管理。
- 全量系统设置。
- 全量短信。
- 全量导入导出。
- 复杂打印模板重写。

## 7. 前端和后端契约

所有页面调用 `/api/admin/**` 后台管理 API。

统一返回结构：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {}
}
```

分页结构：

```json
{
  "items": [],
  "page": 1,
  "pageSize": 20,
  "total": 100
}
```

前端展示状态必须使用后端返回的 `statusLabel`，不要在页面硬编码所有状态。链路追踪由 SkyWalking 负责，前端不依赖链路追踪编号字段；联调排查时记录接口路径、请求时间、订单号、操作人和错误码。

## 8. 验收标准

- `npm run build` 通过。
- 订单主链路页面可以通过 URL 直接访问。
- 页面刷新后路由状态不丢失。
- 订单列表、详情、审方、调剂、复核能调用真实接口。
- 表格、按钮、弹窗在常见桌面宽度下不重叠。
- 错误信息能显示后端返回的业务提示；排障信息通过 SkyWalking、服务日志和订单号关联。
